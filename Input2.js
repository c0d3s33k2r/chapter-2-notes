const readLine = require('readLine-sync');


//1
let num1 = Number(readLine.question("ENTER #:"));
let num2 = Number(readLine.question("ENTER #:"));
console.log(`$(numb) + $(numb2) = $(numb + numb2)`);